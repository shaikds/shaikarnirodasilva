# CLAUDE.md — חוקת הפרויקט

קובץ זה נטען אוטומטית בכל סשן. הוא **מקור האמת** לכללי העבודה.
כל סוכן (sub-agent) וכל משימה כפופים למה שכתוב כאן.

---

## עקרון העל — אסור להמציא

כל החלטה — קוד, מבנה, התנהגות, שם — חייבת להישען על מסמך קאנוני בתיקיית `docs/`.

- אין מסמך שמכסה את המשימה? → **עוצרים**, כותבים אפיון, ורק אז ממשיכים.
- יש סתירה בין הקוד למסמך? → **המסמך מנצח**. מתקנים את הקוד (או — אם ההחלטה השתנתה — מעדכנים את המסמך *במפורש* ומתעדים החלטה).
- AI מציע משהו שלא מופיע באפיון? → זה דגל אדום. מאשרים מול המשתמש לפני שממשיכים.

## מבנה התיעוד (המסמכים הקאנוניים)

| מסמך | תפקיד |
|---|---|
| `docs/00-product/PRD.md` | מה המוצר עושה ולמי. ה"למה". |
| `docs/01-architecture/architecture.md` | מבנה טכני: מודולים, נתונים, גבולות, אינטגרציות. |
| `docs/02-modules/<module>.md` | אפיון מפורט לכל מודול. ה"איך". |
| `docs/03-decisions/<NNN>-<title>.md` | החלטות ארכיטקטורה (ADR): למה בחרנו X ולא Y. |

## הסוכנים (`.claude/agents/`)

| סוכן | תפקיד | מתי |
|---|---|---|
| `architect` | מאשר שינויים מול הארכיטקטורה, מעצב מבנה. | החלטה מבנית, מודול חדש. |
| `spec-writer` | הופך רעיון גולמי לאפיון מודול מסודר. | רעיון חדש לפני קוד. |
| `code-reviewer` | בודק קוד מול האפיון. קריאה בלבד. | אחרי כל משימת קוד. |
| `qa-tester` | כותב ומריץ בדיקות, חושב על מקרי קצה. | אחרי שהקוד עבר ביקורת. |
| `consistency-checker` | עוצר אם יש סטייה מהמסמכים. קריאה בלבד. | לפני commit. |

## תהליך העבודה (הלולאה)

1. **רעיון** → `spec-writer` כותב אפיון תחת `docs/02-modules/`.
2. **אפיון** → `architect` מאשר שהוא משתלב בארכיטקטורה.
3. **פירוק** → אפיון מאושר מתפרק למשימות זעירות. משימה = שינוי אחד, ניתן לבדיקה.
4. **קוד** → מיישמים משימה **אחת בלבד**.
5. **ביקורת** → `code-reviewer` בודק מול האפיון.
6. **בדיקות** → `qa-tester` מכסה את המשימה במבחנים.
7. **בלם** → `consistency-checker` מאשר שאין סטייה. אם יש — עוצרים.
8. **commit** → קטן וברור. branch אחד למשימה.

## כללי קוד

> מלא כאן לפי הסטאק האמיתי שלך (שפה, פריימוורק, סגנון, naming).

- כל פונקציה / endpoint ציבורי — בדיקה אחת לפחות.
- אין secrets בקוד. ערכים רגישים ב-env בלבד; דוגמאות ב-`.env.example`.
- קוד קריא על פני קוד "חכם". בלי הפשטות שלא נדרשות עכשיו.

## כללי git

- branch לכל משימה: `feat/<module>-<task>` או `fix/<...>`.
- הודעת commit: שורה אחת, לשון ציווי, מסבירה את ה"למה".
- אין push ישיר ל-`main`.

## מתי לעצור ולשאול את המשתמש

- חסר אפיון, או יש סתירה בין מסמכים.
- החלטה ארכיטקטונית חדשה: בסיס נתונים, ספרייה חיצונית, שינוי גבולות מודול.
- כל דבר שנוגע ל**אבטחה, תשלומים, או הרשאות**.
- כשבדיקה נכשלת ולא ברור למה.

## אוטומציה — בלם קשיח (hooks)

הסוכן `consistency-checker` *מדווח*. הבלם ה**אוטומטי** הוא hook —
סקריפט דטרמיניסטי שרץ אחרי כל שינוי ומחזיר שגיאה אם בדיקה נכשלת.
ראו `.claude/hooks/README.md` לחיווט מול הבדיקות האמיתיות של הפרויקט.
