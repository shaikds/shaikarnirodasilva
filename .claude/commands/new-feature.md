---
description: הופך רעיון גולמי לאפיון מודול מאושר
argument-hint: <תיאור הרעיון>
---

רעיון חדש לעיבוד: **$ARGUMENTS**

בצע את שלבי האפיון לפי `CLAUDE.md`:

1. הפעל את הסוכן `spec-writer` כדי להפוך את הרעיון לאפיון מודול תחת `docs/02-modules/`.
2. אם חזרו שאלות פתוחות — עצור והצג אותן למשתמש לפני שתמשיך.
3. כשהאפיון מוכן, הפעל את הסוכן `architect` כדי לאשר שהוא משתלב בארכיטקטורה.
4. אם הארכיטקט דחה או ביקש שינויים — תקן את האפיון וחזור לשלב 3.
5. סכם: נתיב האפיון, רשימת המשימות (M1, M2...), והאם הוא מאושר ומוכן לקוד.

אל תכתוב קוד בפקודה הזו.
