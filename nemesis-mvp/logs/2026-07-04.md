# Work log — 2026-07-04

*(Boundary note: the FR-4.7 visual/feel enhancement pass logged at the tail
of `2026-07-03.md` was completed and committed in this session's early hours
— it belongs to this date's session by wall clock.)*

## P5 closed — Genesis Flow in the full zone
- Built: `core/states.js` — the GameFlow state machine
  (BOOT → TUTORIAL → APPROACH → FIRST_BLOOD → FORCED_ESCAPE → FREE_ROAM ⇄
  ENCOUNTER → AFTERMATH). Owns who fights when: `flow.combatEnabled` gates
  resolver pairs + projectiles in the main loop; the rival agent is enabled
  only in FIRST_BLOOD/ENCOUNTER. Tests that exercise systems in isolation
  set `flow.enabled = false`, which makes the flow fully inert (combat
  always live) — the missing piece of the isolation convention started
  earlier.
- Tutorial (FR-1.1): six gated steps (move / lock-on / light / heavy /
  block-hold / dodge) with per-step prompts; attack steps require landing
  on the dummy (whiff-gated); completion logs `tutorial_complete`;
  returning players (rival in storage) boot straight to free roam.
- First Blood (FR-1.2): rival attack ×3 / HP ×2 / agent skill floor 0.9
  (new `skillFloor` on RivalAgent, applied above the rubber band), gate
  sealed, name card announced, auto-lock onto the rival.
- Forced escape (FR-1.3): both fighters HP-clamped at 12 (death impossible),
  15% trigger on EITHER side; player branch = frozen combat + RUN prompt +
  gate opens + exit trigger past z<-15; rival branch = the rival flees along
  the waypoint graph to its base. Genesis resolution writes the canonical
  ledger entries (exact PRD label for `player_fled_first_duel`), applies
  hate (+25 / +40 humiliation via the HATE map), sets `rivalryStarted`,
  restores real stats.
- Steady loop: free-roam proximity (<8m, after a 2s grace) starts an
  encounter anywhere in the zone (AC-5.1.4); encounter ends by death
  (manager.onDuelEnd + aftermath respawns — player at the arena gate, rival
  revived at its base with fresh level/tags) or by breaking away >25m
  (`player_escaped_duel` logged, rival walks home).
- New engine hooks: `Fighter.revive(pos)`, `RivalAgent.skillFloor`.
- **Test findings worth remembering:**
  1. A 1-HP rival *blocked* the intended kill shot — the low-HP rubber band
     maxing defense is the sync system working; the test now mashes several
     attacks at 0.5 HP (blocked chip damage still lethal) instead of
     assuming one clean hit.
  2. Forcing the rival-flees branch while the *player* was also under 15%
     routed to the player branch (correct priority order); test now heals
     the player first.
  3. The rival-flees resolution can complete inside a single probe window
     when the player is far away — asserted via the sessionLog transition
     record instead of sampling live state.
- **Verification:** p5_genesis 21/21 PASS ×2 (full scripted genesis
  playthrough both branches, whiff gating, <90s tutorial, sealed arena,
  hp clamp, ledger labels, encounter/kill/escape bookkeeping, aftermath
  respawns, returning-player skip, 60s random-input bounds soak, nav
  connectivity). Full regression p0–p5 PASS.