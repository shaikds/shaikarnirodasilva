# ארכיטקטורה

> המבנה הטכני הכולל. כל מודול חדש חייב להשתלב במה שכתוב כאן.
> שינוי במסמך הזה = החלטה ארכיטקטונית → מתעדים ADR ב-`docs/03-decisions/`.

## 1. סקירה כללית

תיאור בפסקה: מה הרכיבים הגדולים ואיך הם מדברים זה עם זה.

```
[ לקוח / אפליקציה ]  ->  [ API / Backend ]  ->  [ בסיס נתונים ]
                                  |
                          [ שירותים חיצוניים ]
```

## 2. הסטאק

- **Frontend:** ...
- **Backend:** ...
- **בסיס נתונים:** ...
- **תשתית / הרצה:** Docker / ...

## 3. המודולים

כל מודול = יחידה עצמאית עם אחריות אחת ברורה.

| מודול | אחריות | תלוי ב־ |
|---|---|---|
| auth | התחברות, הרשאות | — |
| ... | ... | ... |

## 4. מודל הנתונים

הישויות המרכזיות והקשרים ביניהן. (טבלאות / קולקשנים, שדות מפתח.)

## 5. גבולות המודולים

מי מותר לו לקרוא למי. מה אסור. מודול לא ניגש ישירות לנתונים של מודול אחר —
רק דרך הממשק הציבורי שלו.

## 6. אינטגרציות חיצוניות

| שירות | לשם מה | סוד / מפתח |
|---|---|---|
| ... | ... | ב-env |

## 7. אבטחה

- אימות והרשאות: ...
- נתונים רגישים: היכן מוצפנים / נשמרים.
- אין secrets בקוד.

## 8. משתני סביבה (ENV)

רשימת המשתנים הנדרשים. הערכים עצמם — לא כאן, ב-`.env` המקומי בלבד.
